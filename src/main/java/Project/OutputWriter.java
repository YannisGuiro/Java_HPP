package Project;

public class OutputWriter implements Runnable{

// On supposera qu'il n'y a qu'un consumer pour ne pas avoir 400 parametres d'entrée
    @Override
    public void run() {

   /*     while (true) {
            Person receivedPerson = null;
            while (receivedPerson == null) {
                receivedPerson = this.queue.poll();
            }

            if (receivedPerson.equals(Producer.POISON_PILL_PERSON)) {
                break;
            } else {
                // TODO: Write in csv
            }

        }
*/
    }
}
